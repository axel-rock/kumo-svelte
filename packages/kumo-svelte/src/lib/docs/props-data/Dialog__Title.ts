import type { PropRow } from '../prop-types';

const rows: PropRow[] = [
  {
    "prop": "render",
    "type": "Snippet | Component",
    "required": false,
    "description": "Custom title render target."
  }
];

export default rows;
