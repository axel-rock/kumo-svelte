import type { PropRow } from '../prop-types';

const rows: PropRow[] = [
  {
    "prop": "text",
    "type": "string",
    "required": true,
    "description": "Text copied to the clipboard."
  }
];

export default rows;
